pipeline {
    agent any

    stages {
        stage('Initialize') {
            steps {
                echo 'initializing pipeline...'
            }
        }
        stage('Taking Actions') {
            steps {
                print "${params.Action}ing ${params.OS}"
                //echo '${params.Action}ing ${params.OS}'
            }
        }
    }
}
